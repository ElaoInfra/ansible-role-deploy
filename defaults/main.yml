---

# Dir
elao_deploy_dir: /var/www

# Dirs
elao_deploy_dir_releases: "{{ elao_deploy_dir }}/releases"
elao_deploy_dir_shared:   "{{ elao_deploy_dir }}/shared"
elao_deploy_dir_current:  "{{ elao_deploy_dir }}/current"

# Shared
elao_deploy_shared_files: []
elao_deploy_shared_dirs:  []

# Releases
elao_deploy_releases: 3

# Strategy
elao_deploy_strategy: git

# Strategy - Git
elao_deploy_git_repo:    git@github.com:foo/bar.git
elao_deploy_git_version: master
